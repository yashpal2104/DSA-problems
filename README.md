# DSA Practice – Fruit Into Baskets

Problem (common variant): Given an array of fruit types, pick the longest contiguous segment that contains **at most 2 distinct** types.

## Run

```bash
python3 main.py << 'EOF'
1 2 3 2 2
EOF
```

Expected output:
- `brute=4`
- `optimal=4`
